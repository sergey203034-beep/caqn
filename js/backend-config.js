/* ===================================================================
   Настройки подключения к общей базе данных сайта (Supabase)
   -------------------------------------------------------------------
   Сайт использует бесплатный Supabase (PostgreSQL + мгновенный
   realtime) в качестве общей базы данных. Дашборд и API Supabase
   доступны из России без VPN на бесплатном тарифе (карта не нужна —
   она нужна только для платных тарифов).

   Как включить общий режим — см. подробную инструкцию в файле
   КАК_СДЕЛАТЬ_САЙТ_ОБЩИМ.md. Коротко:
   1. Зарегистрируйтесь на https://supabase.com (можно через GitHub).
   2. Создайте новый проект (Free tier).
   3. В разделе SQL Editor выполните файл supabase-schema.sql из
      этого проекта — он создаст все нужные таблицы и включит realtime.
   4. В разделе Project Settings → API скопируйте:
      - "Project URL" → вставьте в SUPABASE_URL ниже;
      - "anon public" ключ → вставьте в SUPABASE_ANON_KEY ниже.

   Пока SUPABASE_URL ниже не заполнен реальным адресом, сайт работает
   в демо-режиме (данные видны только в этом браузере) — ничего не
   ломается, просто общий доступ ещё не подключён.
   =================================================================== */

const SUPABASE_URL = 'https://vkehczmkrkdpkkyrkssj.supabase.co';
const SUPABASE_ANON_KEY = 'sb_publishable_ZP0V9JZ9M4PfTbVYFgNl1A_9EEhWulo';
