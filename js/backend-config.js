/* ===================================================================
   Настройки подключения к общей базе данных сайта
   -------------------------------------------------------------------
   Вместо зарубежного Supabase сайт теперь использует свой собственный
   маленький бэкенд на PHP + MySQL (файлы backend/api.php и
   backend/db-schema.sql) — такой бэкенд работает на любом российском
   хостинге с поддержкой PHP и MySQL (бесплатные тарифы есть, например,
   у SpaceWeb и 2domains — см. КАК_СДЕЛАТЬ_САЙТ_ОБЩИМ.md).

   Как включить общий режим:
   1. Загрузите папку backend/ на хостинг рядом с сайтом (или в
      отдельную папку — тогда пропишите её адрес ниже).
   2. Впишите данные MySQL-базы в backend/db-config.php.
   3. Импортируйте backend/db-schema.sql через phpMyAdmin на хостинге.
   4. Впишите ниже адрес, по которому открывается api.php, например:
        https://ваш-сайт.ru/backend/api.php
      (без "/api.php" на конце — только адрес папки backend).
   5. Придумайте и впишите любой BACKEND_KEY (простой пароль от
      подделки запросов) — тот же пароль впишите в backend/db-config.php.

   Пока BACKEND_URL ниже не заполнен реальным адресом, сайт работает в
   демо-режиме (данные видны только в этом браузере) — ничего не
   ломается, просто общий доступ ещё не подключён.
   =================================================================== */

const BACKEND_URL = 'https://ВАШ-САЙТ.ru/backend';
const BACKEND_KEY = 'придумайте-свой-пароль';
