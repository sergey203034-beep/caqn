-- Нужно выполнить только один раз, если таблицы в Supabase уже были
-- созданы РАНЬШЕ этого обновления (шифрование паролей). Если вы ещё
-- не создавали таблицы — просто используйте обычный supabase-schema.sql,
-- этот файл не нужен.
--
-- Как использовать: Supabase → ваш проект → SQL Editor → New query →
-- вставьте это целиком → Run.

alter table users add column if not exists salt text;
